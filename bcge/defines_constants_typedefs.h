#ifndef DEFINES_H
#define DEFINES_H

#include <vector>
#include <string>

using namespace std;

// Defines
#define TITLE "SFML Test v0.1"

// Typedefs
typedef unsigned char uchar;
typedef unsigned int uint;
typedef vector<string> StringVec;

// Constants
static const float dt = 0.1f; // How fast the physics is updated (sec)

#endif
